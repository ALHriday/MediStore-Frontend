import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["lh3.googleusercontent.com", "https://img.icons8.com", "https://www.pexels.com", "images.pexels.com"],
  },
};

export default nextConfig;
